# 截图

`aquarium.png` 是根目录 README 使用的主截图（2560×1319，Retina 2x，约 155 KB）。

换图时保持同名同格式即可，README 不用改。两点注意：

- **保持小体积**：README 图片控制在 1 MB 以内。Retina 截图（2x）在 GitHub 上会缩放显示，不需要缩到 1x，但也别把 5 MB 的原图直接塞进仓库——`sips -Z 2560 aquarium.png` 可以等比缩放到宽 2560。
- **公开仓库**：截图里容易出现会话标题、工作区路径、侧边栏会话列表。发布前对着图看一遍。
